name: guven_mobile
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # Renders the Güvən Finans vector logo (SVG) used by the onboarding build-up
  # animation. Flutter has no native SVG support, so this package is required.
  flutter_svg: ^2.0.10
  video_player: ^2.10.0
  liquid_glass_easy: ^3.5.0
  http: ^1.6.0
  flutter_secure_storage: ^10.3.1

  # A task's attachments are downloaded to the app's cache and handed to
  # whatever the phone already uses to open that type — the way a file sent in
  # WhatsApp opens once it has been downloaded. `open_filex` ships its own
  # Android `FileProvider` so a cached file can be shared out without any
  # manifest work here, and uses `UIDocumentInteractionController` on iOS.
  path_provider: ^2.1.5
  open_filex: ^4.7.0
  liquid_glass_renderer: 0.2.0-dev.4
  flutter_liquid_glass_plus: ^0.0.7
  native_glass_navbar: ^1.0.2
  record: ^7.1.1
  audioplayers: ^6.8.1
  file_picker: ^12.1.2
  http_parser: ^4.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  video_player_platform_interface: ^6.9.0
  plugin_platform_interface: ^2.1.8

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/logos/logo.svg
    - assets/images/background/home_aurora.svg
    - assets/images/icons/nav_icons/
    - assets/images/icons/task_list_selection_icons/
    - assets/images/icons/new_task_icons/
    - assets/images/icons/edit_icons_different_ones/
    - assets/videos/onboarding_background.mp4
    - assets/videos/page_3_icons/

  fonts:
    - family: CalSans
      fonts:
        - asset: assets/fonts/CalSans-Regular.ttf
          weight: 400
    - family: Poppins
      fonts:
        - asset: assets/fonts/Poppins-Regular.ttf
          weight: 400
        # The task cards set their timestamps and deadlines in italics, the way
        # the design does. Without a real italic face the engine slants the
        # upright one, which shears the diacritics off `ə` and `ş`.
        - asset: assets/fonts/Poppins-Italic.ttf
          weight: 400
          style: italic
        - asset: assets/fonts/Poppins-Medium.ttf
          weight: 500
        # Not used by any screen directly — the bottom nav bar's own item style
        # asks for w600 on the selected label, and without a real face for it
        # the engine fakes one by smearing the regular.
        - asset: assets/fonts/Poppins-SemiBold.ttf
          weight: 600
        # The executor's name on a task card is bold when it is the signed-in
        # user's own — that is the whole signal that the task is theirs.
        - asset: assets/fonts/Poppins-Bold.ttf
          weight: 700

  shaders:
    - shaders/aurora.frag

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies,
  # see https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
